<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rabson.ea">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="helpers.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MasterLogin.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ListViewHandler.TeacherActivity" />
        <!-- <activity android:name=".Attendance.AttendanceActivity" /> -->
        <activity android:name=".AcademicYear.AcademicActivity" />
        <activity android:name=".Classes.ClassActivity" />
        <activity android:name=".Students.StudentActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AcademicYear.SetAcademicYearActivity" />
        <activity android:name=".StudentProgress.ProgressActivity" />
        <activity android:name=".Attendance.StudentAttendanceActivity" />
        <activity android:name=".Report.ReportActivity" />
        <activity android:name=".ClassTeacherDash.ClassTeacherActivity" />
        <activity android:name=".Classes.ClassListActivity" />
        <activity android:name=".Students.StudentRegistrationActivity" />
        <activity android:name=".ListViewHandler.RegisterTeacherActivity" />
        <activity android:name=".Attendance.MorningAttendace"></activity>
    </application>

</manifest>